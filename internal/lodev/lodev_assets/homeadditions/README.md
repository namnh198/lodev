#lodev-generated

File in .lodev/homeadditinons will be copied to the web container's home directory for all projects.

An example bash_aliases.example is provided here. To make this file active you can either

```
cp bash_aliases.example .bash_aliases
```

or

```
ln -s bash_aliases.example .bash_aliases
```
