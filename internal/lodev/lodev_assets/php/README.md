#lodev-generated

\*.ini files in `.lodev/php` are added the project's PHP configuration.

For example, if you rename the provided `php-example.ini.example` to `php-example.ini`, it's configuration will be added to your PHP configuration.

```
[PHP]
max_execution_time = 240
```
